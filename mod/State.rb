# encoding: utf-8

# This module contains shortcuts to the States of a humanoid character. Respectively IDLE (stoped) and MOV (in movement).
module State
	IDLE = 0 unless defined? IDLE
	MOV = 1 unless defined? MOV
end
